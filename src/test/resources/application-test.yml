micronaut:
  security:
    endpoints:
      oauth:
        enabled: true
    token:
      jwt:
        signatures:
          secret:
            generator:
              secret: "localSignatureGeneratorSecretKey"
        generator:
          refresh-token:
            secret: "localRefreshTokenGeneratorSecret"

logger:
  levels:
    io.micronaut: INFO
